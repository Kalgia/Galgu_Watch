# Galgu Watch — 작업계획서

> 버튼 하나로 공부 시간을 재고, 매일의 기록이 캘린더에 쌓이고,
> 스크린샷과 작업일지로 하루를 돌아보는 개인용 Windows 데스크톱 앱.

## 1. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 프로젝트명 | Galgu Watch |
| 형태 | Windows 데스크톱 앱 — 트레이 상주 + 팝업 창 |
| 사용자 | 본인 1명. 서버 없음, 모든 데이터는 내 PC에 로컬 저장 |
| 핵심 가치 | 기록에 드는 마찰을 0으로 — 누르면 시작, 누르면 끝. 저장·정리·통계는 앱이 알아서 |

## 2. 핵심 기능 명세 (요구사항 정리)

### 2.1 스탑워치
- 큰 토글 버튼 하나: 클릭 → 측정 시작, 다시 클릭 → 정지.
- 정지할 때마다 **세션** 1개 저장 (시작 시각, 종료 시각, 지속 시간).
- 하루 총 공부시간 = 그날 세션들의 합. 현재 세션 시간과 오늘 누적 시간을 함께 표시.
- 기록이 깨지지 않게 하는 처리:
  - 시간은 시작 시각과 현재 시각의 차로 계산 — 백그라운드에서 타이머가 밀려도 오차 없음.
  - 측정 중 30초마다 진행 상태를 저장 → 앱이나 PC가 갑자기 꺼져도 복구.
  - PC 절전·잠금 시 자동 일시정지 — 절전 시간이 공부시간에 섞이는 것 방지.
  - 자정 넘겨 공부하는 경우 대비 **"하루 시작 시각"** 설정 (기본 05:00) — 새벽 2시 공부는 전날 기록으로 집계.

### 2.2 캘린더 기록 뷰
- 월간 캘린더 그리드. 각 날짜 칸에 그날 총 공부시간 + 시간에 비례한 색 농도(히트맵).
- 상단에 이번 달 합계 / 일평균 / 공부한 날 수.
- 날짜 클릭 → **일별 상세** 화면:
  - 세션 타임라인 — 몇 시부터 몇 시까지 공부했는지 하루 축 위에 막대로 표시.
  - 그날 스크린샷 갤러리.
  - 그날 작업일지 바로 열람·수정.

### 2.3 스크린샷
- **주기 캡처**: 타이머가 작동 중일 때만, 설정한 간격(기본 10분)마다 자동 캡처.
- **수동 캡처**: 앱 내 버튼, 미니 오버레이의 📷 버튼, 트레이 메뉴로 실행. 캡처 직후 한 줄 메모를 붙일 수 있음.
- 저장 정책:
  - WebP 압축 저장 (PNG 대비 용량 1/5 수준). 예상 용량: 하루 6시간 · 10분 간격 기준 약 5~15MB/일.
  - 경로: `data/screenshots/2026-08-05/1430_auto.webp`
  - 보관 기한(기본 90일) 지나면 자동 삭제 — 설정에서 조절·해제 가능.
- 다중 모니터: 기본은 주 모니터만 캡처, 설정에서 전체 모니터로 변경 가능.

### 2.4 작업일지 (노트)
- 하루 1개 문서, 일기 형식. 마크다운 지원.
- 그날 스크린샷 갤러리에서 클릭 한 번으로 본문에 이미지 삽입.
- 새 일지를 열면 자동 템플릿 — 날짜, 총 공부시간, 세션 요약이 미리 채워진 상태에서 쓰기 시작.
- 캘린더 일별 상세와 연결 — 캘린더에서 바로 그날 일지로 이동.

## 3. 추가 기능 추천

### v1 포함 확정 (2026-08-05 결정)

| 기능 | 설명 | 이유 |
|---|---|---|
| **자리비움 자동 일시정지** | 마우스·키보드 입력이 N분(기본 5분) 없으면 자동 일시정지, 돌아오면 알림 | 켜놓고 자리 비운 시간이 섞이면 기록 전체를 못 믿게 됨. 추가 기능 중 1순위 |
| **미니 오버레이** | 화면 구석에 항상 떠 있는 작은 타이머 + 시작/정지·📷 캡처 버튼 (드래그 이동, 투명도 조절) | 시간이 눈에 보이면 긴장감 유지. 창을 찾지 않아도 클릭 한 번으로 조작 — 전역 단축키의 역할을 대신함 |
| **트레이 상주** | 창을 닫아도 종료되지 않고 트레이에 상주 | 실수로 꺼서 기록이 끊기는 것 방지 |
| **일일 목표 + 스트릭** | 하루 목표 시간 설정, 달성일은 캘린더에 표시, 연속 달성일 카운트 | 갈구 워치라는 이름값. 꾸준함을 만드는 장치 |

제외 결정 (2026-08-05):
- **전역 단축키** — 다른 프로그램의 단축키와 겹칠 수 있어 제외. 수동 캡처의 단축키도 같은 이유로 빼고, 오버레이·트레이의 버튼으로 대체.
- **부팅 시 자동 실행** — 제외. 앱은 직접 실행하며, 실행 후에는 트레이에 상주.

### 이후 확장 아이디어

| 기능 | 설명 |
|---|---|
| 과목/태그 | 세션마다 태그(수학, 영어…)를 달고 태그별 통계 |
| 통계 대시보드 | 주간/월간 추이 그래프, 시간대별 분포 — "나는 몇 시에 제일 공부하나" |
| 연간 잔디 뷰 | GitHub 잔디 스타일의 1년 히트맵 |
| 뽀모도로 모드 | 25/5 사이클 + 휴식 알림 |
| 타임랩스 | 하루 스크린샷을 이어붙여 GIF/영상 생성 → 일지에 첨부 |
| 사용 앱 기록 | 캡처 시점의 활성 창 제목 저장 — "그 시간에 뭘 했나" 자동 기록 |
| 주간 리포트 | 일요일마다 한 주 요약을 일지에 자동 생성 |
| 데이터 내보내기 | CSV/JSON 내보내기 + DB 자동 백업 |
| 다크 모드 | 시스템 테마 연동 |

## 4. 기술 스택 (확정 — 2026-08-05)

**C# .NET(WPF) + WebView2 하이브리드.** 설계 원칙: **"작업 중에는 네이티브만 상주한다."**
ZBrush · 3ds Max · Substance Painter 같은 무거운 DCC와 항상 같이 돌아가야 하므로, 하루 종일 떠 있는 부분(타이머·오버레이·트레이·캡처)은 C# 단일 프로세스(약 40~70MB)로 만들고, 캘린더·작업일지 같은 열람 화면만 열 때 WebView2로 띄웠다가 닫으면 메모리에서 완전히 내린다.

| 구성 | 선택 | 근거 |
|---|---|---|
| 상주부 (타이머·트레이·오버레이·캡처) | C# WPF, 프로세스 1개 | 작업 중 부담 최소. 오버레이는 포커스를 뺏지 않는 창(NOACTIVATE) — DCC 작업 방해 없음 |
| 열람부 (캘린더·일지) | WebView2 — 열 때 생성, 닫으면 완전 해제 | 웹 품질 UI를 쓰되 안 볼 땐 0MB. 런타임은 Win11 기본 탑재 |
| DB | SQLite (Microsoft.Data.Sqlite) | 파일 하나 |
| 스크린샷 | GDI 캡처 + ImageSharp WebP 인코딩 | 인코딩은 백그라운드 스레드에서 |
| 자리비움·절전·잠금 감지 | Windows 내장 API (GetLastInputInfo, SystemEvents) | 추가 의존성 없음 |
| 열람 UI 프런트엔드 | Vite + React + TypeScript (정적 빌드) | 캘린더 히트맵 · 마크다운 일지 |
| 빌드 | dotnet publish (.NET 9 SDK — 이미 설치돼 있음) | 추가 툴체인 설치 0 |

탈락 기록:
- **Electron** (1차안) — 상시 150~300MB. DCC 병행 환경에 부적합.
- **Tauri 2** — 설치 파일은 가장 작지만 Rust + VC++ 빌드도구 약 5GB 설치가 필요하고, 오버레이가 웹뷰라 작업 중 상시 메모리는 하이브리드보다 오히려 높음.
- **순수 WPF** — 상주 메모리는 동급이나 캘린더·일지 UI 품질에서 손해. 열람 화면은 잠깐씩만 여니 하이브리드가 이득.

## 5. 데이터 설계

```
<앱 데이터 폴더>/
  galgu.db                  # SQLite — 세션·노트·설정 전부
  screenshots/
    2026-08-05/
      0930_auto.webp
      1102_manual.webp
  backups/                  # DB 자동 백업 (최근 7개 보관)
```

SQLite 테이블:

| 테이블 | 컬럼 | 비고 |
|---|---|---|
| sessions | id, date, started_at, ended_at, duration_sec, tag_id | date는 "하루 시작 시각" 기준 날짜 |
| screenshots | id, date, taken_at, kind(auto/manual), file_path, memo, session_id | |
| notes | date(PK), content_md, updated_at | 하루 1문서 |
| tags | id, name, color | 확장용 |
| settings | key, value | 캡처 간격, 하루 시작 시각 등 |

## 6. 화면 구성

```
[메인 창 — 타이머]                      [트레이 아이콘]
┌────────────────────────────┐          우클릭 메뉴: 시작/정지,
│  2026-08-05 (화)           │          수동 캡처, 창 열기, 종료
│                            │
│        01:23:45            │  ← 현재 세션
│     오늘 누적 3:42:10       │
│                            │
│     [    ⏸  정지    ]      │  ← 큰 토글 버튼
│                            │
│   📷 캡처  📅 캘린더  📝 일지  │
└────────────────────────────┘

[캘린더]
┌────────────────────────────────────┐
│ ◀ 2026년 8월 ▶      합계 68:20 · 평균 2:12 │
│  일   월   화   수   목   금   토     │
│           [4]  [5]  ...             │  칸마다: 날짜 + 3:42 + 색 농도
│  (날짜 클릭 → 일별 상세)              │
└────────────────────────────────────┘

[일별 상세]
┌────────────────────────────────────┐
│ 8월 5일 (화) — 총 3:42:10, 세션 4개   │
│ 타임라인   ░░▓▓░░▓▓▓░░░▓▓░░         │
│ 스크린샷   [img] [img] [img] …      │  클릭 → 크게 보기 / 일지에 삽입
│ ── 작업일지 ──────────────────      │
│ (마크다운 에디터)                    │
└────────────────────────────────────┘

[미니 오버레이 — 항상 위, 화면 구석]
┌────────────────┐
│ ⏸ 01:23:45  📷 │   시간 클릭: 시작/정지 · 📷: 수동 캡처 · 드래그로 이동
└────────────────┘
```

## 7. 개발 로드맵

| Phase | 내용 | 완료 기준 |
|---|---|---|
| 0 | 네이티브 셸 | WPF 앱이 트레이 + 미니 오버레이로 상주하고 DB에 연결됨 |
| 1 | 스탑워치 코어 | 오버레이에서 시작/정지, 세션 저장, 크래시 복구, 절전·잠금·자리비움 자동 정지 |
| 2 | 스크린샷 | 주기 + 수동 캡처, WebP 저장, 보관 기한 자동 정리 |
| 3 | 캘린더 (웹 UI) | WebView2를 열 때만 생성, 월간 히트맵, 일별 상세(세션 타임라인·갤러리) |
| 4 | 작업일지 (웹 UI) | 마크다운 에디터, 자동 템플릿, 스크린샷 삽입 |
| 5 | 마감 | 설정 화면, 일일 목표 + 스트릭, 배포본 만들기 |
| 6+ | 확장 | 3장의 확장 아이디어 중 선택 |

각 Phase가 끝날 때마다 실제로 실행해서 확인하고 다음으로 넘어간다.

**진행 상황 (2026-08-06): Phase 0~5 완료.** 실측 — Release 빌드 상주 상태(오버레이+트레이) 실제 점유 메모리 약 11MB(유휴 트림 후), 캘린더 창을 여는 동안만 WebView2 프로세스가 추가로 뜨고 닫으면 회수됨.
- Phase 5에서 추가된 것: ⚙️ 설정 화면(캡처 간격·보관 기한·자리비움·하루 시작 시각·캡처 대상 모니터·오버레이 투명도·일일 목표), 일일 목표 달성 ✓ 표시 + 연속 달성 🔥 스트릭, 다크/화이트 테마 전환(🌙/☀️), 전용 앱 아이콘, 바탕화면 바로가기.
- 다음 확장: 9장(공유 기능) 및 3장의 확장 아이디어.

## 8. 주의할 점과 열어둔 결정

주의할 점:
- **스크린샷 속 민감정보** — 공부 중 비밀번호 입력 화면 등이 찍힐 수 있음. 갤러리에서 개별 삭제를 쉽게 만들고, 특정 앱 제외 기능은 확장으로 고려.
- **디스크 용량** — WebP 압축 + 보관 기한으로 관리 (2.3절 정책).

확정 전 열어둔 값 (기본값으로 진행하되 언제든 변경 가능):

| 항목 | 제안 기본값 |
|---|---|
| 기술 스택 | ~~Electron~~ → **C# + WebView2 하이브리드 확정** (4장) |
| 주기 캡처 간격 | 10분 |
| 다중 모니터 | 주 모니터만 |
| 하루 시작 시각 | 05:00 |
| 스크린샷 보관 기한 | 90일 |
| 자리비움 판정 시간 | 5분 |

→ 위 값들은 모두 앱의 ⚙️ 설정 화면에서 직접 바꿀 수 있게 구현됨 (2026-08-06).

## 9. 공유 기능 계획 — 서로의 일지를 보며 작업하기 (2026-08-06)

목표: 내 작업일지를 다른 사람과 공유하고, 서로의 일지·작업 시간을 보며 동기를 유지한다.
지금 앱은 100% 로컬(서버 없음)이므로 단계적으로 확장한다.

### 1단계 — 일지 내보내기 + 수동 공유 ✅ 구현됨 (2026-08-06)
- 일별 상세에 **"공유용 내보내기"** 버튼: 그날 일지 + 선택한 스크린샷 + 시간 통계를 이미지가 포함된 **단일 HTML 파일**(또는 이미지 카드 1장)로 생성.
- 디스코드·카톡 등에 파일/이미지로 올려서 공유. 받는 사람은 앱 없이 브라우저로 열람.
- 리스크 없음. "공유 습관이 실제로 생기는지" 검증하는 용도로도 좋음.

### 2단계 — 공유 폴더 구독 (서버 불필요, 친구 2~10명 규모, 구현 2~3일)
- 구글드라이브·드롭박스 등 **공유 폴더를 앱에 지정**.
- **"이 날 공개"** 버튼을 누른 날만: 일지 + 선택한 스크린샷 + 시간 통계가 공유 폴더의 내 이름 폴더로 자동 발행.
- 앱에 **"친구" 탭** 추가: 같은 폴더에 발행된 다른 사람들의 일지를 캘린더/피드 형태로 열람.
- 장점: 서버·계정·운영비 0, 참여자 관리 = 폴더 초대. 단점: 실시간성은 클라우드 동기화 속도에 의존.

### 3단계 — 서버 기반 (본격 확장, 구현 1~2주+)
- Supabase 같은 BaaS 사용 (무료 티어로 시작 가능): 계정, 친구/그룹, 일지 피드.
- 이때 가능해지는 것: **"지금 작업 중" 실시간 표시**(스터디윗미 느낌), 응원 이모지·댓글, 주간 그룹 통계/리더보드, 링크 하나로 일지 공유.
- 대신 생기는 것: 운영 책임(계정·데이터·소액 비용)과 보안 관리.

### 개인정보 원칙 (모든 단계 공통)
- 기본은 **전부 비공개**. 공유는 하루 단위 **옵트인**.
- 스크린샷은 **개별 선택**한 것만 포함 (작업 화면에 민감 정보가 찍힐 수 있으므로).
- 발행 취소(회수) 기능 포함.

### 결정 사항 (2026-08-06, 사용자 답변 반영)
- 공유 채널: **디스코드 또는 카톡** / **실시간성 필요**

→ 확정 경로: **디스코드 중심** (카톡은 공식 API가 없어 자동화 불가 — 카드 수동 공유용)

### 1.5단계 — 디스코드 연동 ✅ 구현됨 (2026-08-06)
| 기능 | 방식 | 효과 |
|---|---|---|
| **"지금 작업 중" 실시간 표시** | Discord Rich Presence (로컬 연동) | 타이머가 돌면 내 디스코드 프로필에 "Galgu Watch — 작업 중 1:23:45"가 실시간으로 뜨고, 친구들 것도 서로 보임. 서버·계정 관리 전부 디스코드가 대신함 |
| **일지 자동 발행** | 채널 웹훅(Webhook) | 버튼 한 번(또는 하루 마감 시)에 공유 카드가 채널에 자동 업로드 |

Application ID·웹훅 URL은 사용자 제공분을 설정 DB에 기록 완료 (⚙️ 설정의 "디스코드 연동" 섹션에서 변경 가능). Rich Presence는 로컬 IPC(named pipe) 직접 구현 — 외부 라이브러리 의존성 없음. 웹훅 연결 테스트 메시지 발송 확인됨.

**포터블 배포본 (2026-08-06):** `dist\GalguWatch\` (self-contained win-x64, .NET 설치 불필요) → 바탕화면 `GalguWatch_포터블_v1.0.zip` (72MB). 앱 ID는 기본값으로 내장(공개 값), 웹훅 URL은 보안상 미포함 — 친구가 설정에서 직접 붙여넣도록 사용법.txt에 안내. 새 버전 배포 시: `dotnet publish -c Release -r win-x64 --self-contained true -o dist\GalguWatch` 후 재압축.

3단계(자체 서버)는 디스코드 밖으로 확장할 필요가 생길 때만 간다.
